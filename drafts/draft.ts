/*const footerTaskLeftLocator = page.getByTestId('footer').locator('span');
let textFromElement = await footerTaskLeftLocator.textContent();

if (textFromElement !== null) {
    const numberFromText = textFromElement.match(/\d+/);
    const number = Number(numberFromText)
    console.log(`Extracted number: ${number}`);
} else {
    console.log('Text content is null')
}*/